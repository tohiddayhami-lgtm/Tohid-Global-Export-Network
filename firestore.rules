rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Live export-network JSON (see networkFirestoreSync.ts). Tighten for production (Auth, App Check).
    match /exportNetwork/{docId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
